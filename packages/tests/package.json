{
  "name": "tests",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "npm run test-jest && npm run test-pw",
    "test-jest": "cross-env NODE_NO_WARNINGS=1 NODE_OPTIONS=--experimental-vm-modules npx jest",
    "test-pw": "cross-env NODE_NO_WARNINGS=1 npx playwright test",
    "test-pw-debug": "cross-env PWDEBUG=1 npm run test-pw"
  },
  "devDependencies": {
    "@playwright/test": "^1.25.0",
    "@types/jest": "^28.1.6",
    "cross-env": "^7.0.3",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4",
    "vite": "^2.9.15"
  }
}
